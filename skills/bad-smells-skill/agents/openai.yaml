interface:
  display_name: "Bad Smell Review"
  short_description: "Deep refactoring-smell code review guidance"
  default_prompt: "Use $bad-smells-skill to review this repository for refactoring bad smells and produce a structured report plus a remediation backlog."
